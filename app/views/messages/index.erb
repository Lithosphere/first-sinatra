<% @messages.each do |message| %>
<div class ="message">
  <h3><font color ="red"><%= message.title %></font> <small>(by: <%= message.author %>)</small></h3>
  <p>
    <%= message.content %>
  </p>
</div>
<div>  <a href="/messages/new">Post a message... It's Free(for now)!</a></div>
<% end %>